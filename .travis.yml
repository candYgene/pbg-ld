language: python
sudo: required
dist: trusty
services:
  - docker
before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y build-essential
  - cd src
install:
  - travis_wait 30 make all
script:
  - python test.py
